# Required
IRODS_PATH=/sodarZone/projects/<project-id>/landing_zones/<user>/<lz>
DAVRODS_SERVER=davrods-anonymous.sodar.cubi.bihealth.org
IRODS_TOKEN=replace-me

# JSON array; keep it on one line for shell compatibility
datasets=[{"name":"Example data set","archive":"DE_pipeline.tar.gz","config":"DE_config.yaml"}]

# Optional
TITLE=SeaPiper example project
IMAGE=ghcr.io/bihealth/seapiper:0.6.0
HOST_PORT=8080
